/**
 * App.tsx — Phase 1 Bootstrap 占位
 *
 * 当前阶段（Phase 1 前端基础）只搭好骨架/样式 token，没有任何 UI 组件。
 * 真正的根布局会在 Phase 1 末尾的 "App.tsx — 根布局" 任务中接管，
 * 届时会渲染 Viewport + ModeBar + SettingsSheet + DropOverlay + ConnectionBar + ToastContainer。
 *
 * 此前保持 null —— 让 globals.css 设置的 body 暗色背景充满整个视口，
 * 既能视觉验证暗色主题生效，又不引入任何未经 Stitch 设计的临时 UI。
 */
function App() {
  return null
}

export default App
